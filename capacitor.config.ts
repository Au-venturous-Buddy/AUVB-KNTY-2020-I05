import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.i02.2019.knty.auvb',
  appName: "Tapsi ni Vivian",
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.i03.2019.knty.auvb',
  appName: "Macao Imperial Tea (Anonas Branch)",
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.i01.2020.knty.auvb',
  appName: "Popeyes Louisiana Kitchen (Robinsons Galleria)",
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
